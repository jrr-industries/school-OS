{
  "name": "schoolos",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @schoolos/web dev",
    "build": "pnpm --filter @schoolos/web build",
    "lint": "pnpm --filter @schoolos/web lint",
    "format": "prettier --write \"**/*.{ts,tsx,json,css,md}\""
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
